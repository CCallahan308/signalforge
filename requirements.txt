# SignalForge runtime dependencies (pipeline + dashboard).
# Mirrors [project].dependencies in pyproject.toml. Target: Python 3.11.
# Upper bounds keep an unvetted major release from silently breaking the build.

# Data + IO
numpy>=1.26.4,<3.0
pandas>=2.2.0,<4.0
pyarrow>=15.0.0          # required by pandas.to_parquet

# Modeling
scikit-learn>=1.4.0,<2.0
scipy>=1.12.0
optuna>=3.5.0
joblib>=1.3.0

# Dashboard
streamlit>=1.57.0,<2.0
plotly>=5.18.0,<7.0

# Logging (download/quickstart scripts)
loguru>=0.7.0
